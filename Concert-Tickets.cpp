#include <bits/stdc++.h>

using namespace std;
long long h[200005], t[200005];
int main(){
    long long n, m;
    cin >> n >> m;
    for(int i = 0; i < n; i++)
        cin >> t[t];
    for(int i = 0; i < m; i++)
        cin >> h[i];
    sort()
}
